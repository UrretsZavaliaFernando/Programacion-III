// console.log("mi primer js")

// let today=new Date()
// console.log(today)

let dd=today.getDate()
let mm=today.getMonth()+1
let yyyy=today.setFullYear()

// console.log(dd+"/"+mm+"/"+yyyy)

console.log(`${dd}/${mm}/${yyyy}`)

function suma(a,b)
{
    if(a===b)
    {
        return (a+b)
    }
    else
    {
        return (a*b)
    }
}
sumar2= function(a,b)
{
    return (a+b)
}

sumar3=(a,b)=>{return (a+b)}

//en java script una funcion es un objeto!!!!


//"node" para ejecutar en terminal

